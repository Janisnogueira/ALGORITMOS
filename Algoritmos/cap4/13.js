let idadetexto = prompt("digite sua idade:")
let idadenumero = 
number(idadetexto);
console.log("sua idade é", idadeNumero);
if (isNaN(idadenumero)) {
    console.log("o valor digitado não é um numero valido.");
}
